{
  "name": "devagent-reviewer-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Safe DevAgent + ReviewerAgent laboratory powered by Groq local tool calling.",
  "scripts": {
    "dev": "node --watch server.mjs",
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check src/agents.mjs && node --check src/workspace.mjs && node --check public/app.js && npm test",
    "smoke": "node scripts/smoke-agent.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
